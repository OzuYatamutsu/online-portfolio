name: Build and Deploy
on:
  push:
    branches:
      - main

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build and Deploy
        run: echo "TODO"
